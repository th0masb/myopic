use crate::board::Board;
use crate::board::Move;
use crate::board::ReversalData;

impl Board {
    pub fn devolve(&mut self, action: Move, evolve_discards: ReversalData) {
        unimplemented!()
    }
}
